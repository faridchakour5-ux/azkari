{
  "name": "salaty-native",
  "version": "1.0.0",
  "private": true,
  "description": "غلاف Capacitor الأصلي لتطبيق صلاتي — أذان الصلوات على مستوى النظام",
  "scripts": {
    "copy:web": "node copy-web.js",
    "sync": "npm run copy:web && npx cap sync android",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/local-notifications": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
